# Security Policy

Do not report vulnerabilities through public issues. Send a private report to the repository maintainer with reproduction steps, impact, and a proposed mitigation if available.

Never include credentials, private keys, customer data, or WorkOS tokens in a report. Local-auth keys belong only in the ignored `.local-auth/` directory.
