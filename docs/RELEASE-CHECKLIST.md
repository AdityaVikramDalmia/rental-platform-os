# Release Checklist

Before making this repository public, run `npm run release:check`. Confirm the working tree contains no private workflow directories, committed secrets, remote, broken local documentation links, or failed quality checks.

Do not publish packages, add a remote, or push as part of this checklist.
